# Local dev database (Docker, tmpfs)
DEV_DB_PORT="5435"
DATABASE_URL="postgresql://postgres:postgres@127.0.0.1:5435/tech_career_readiness_dev"

# Neon Postgres connection string (optional override)
# DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?sslmode=require"

# Auth.js (NextAuth)
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-random-secret"
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# Admin access (comma-separated emails, dev/test only)
ADMIN_EMAILS="admin@example.com"

# Rate limiting (Upstash Redis)
UPSTASH_REDIS_REST_URL="https://example-redis.upstash.io"
UPSTASH_REDIS_REST_TOKEN="replace-with-upstash-token"

# Optional request sizing
MAX_JSON_BODY_BYTES="16384"

# Optional base URL override for request origin checks
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
